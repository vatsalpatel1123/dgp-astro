---
// all-on-x-ai.astro - Updated based on latest client feedback
import '../css/main.css';
import Navigation from '../components/Navigation.astro';
import FooterBar from '../components/FooterBar.astro';
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All-On-X AI | Dentistry Growth Partners</title>
  <meta name="description" content="The #1 Way to Consistently Close Full Arch + Multi-Implant Cases. Infrastructure that consistently attracts and converts high-value implant cases worth $15k-75k on autopilot.">
  <link rel="icon" type="image/png" href="/636f4050ef41c9124a12cbba/dgp-logo1.png" />
  <link rel="shortcut icon" type="image/png" href="/636f4050ef41c9124a12cbba/dgp-logo1.png" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@latest/tabler-icons.min.css">
  
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --primary-light: #EDEDE8;
      --primary-dark: #051023;
      --primary-blue: #07387D;
      --accent-blue: #248EEF;
      --white: #ffffff;
      --black: #1d1d1d;
      --grey: #f5f5f5;
      --dim-grey: #646464;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: var(--white);
      color: var(--black);
      line-height: 1.6;
      overflow-x: hidden;
    }

    .section-container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 24px;
    }

    /* HERO SECTION */
    .hero-section {
      min-height: 85vh;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      overflow: hidden;
      background: linear-gradient(135deg, var(--primary-dark) 0%, var(--primary-blue) 50%, var(--accent-blue) 100%);
      padding: 160px 24px 100px;
    }

    /* Animated gradient orbs */
    .hero-section::before {
      content: '';
      position: absolute;
      top: -150px;
      right: -150px;
      width: 500px;
      height: 500px;
      background: radial-gradient(circle, rgba(36, 142, 239, 0.25) 0%, transparent 70%);
      border-radius: 50%;
      animation: floatOrb 20s ease-in-out infinite;
      z-index: 1;
    }

    .hero-section::after {
      content: '';
      position: absolute;
      bottom: -100px;
      left: -100px;
      width: 400px;
      height: 400px;
      background: radial-gradient(circle, rgba(36, 142, 239, 0.2) 0%, transparent 70%);
      border-radius: 50%;
      animation: floatOrb 25s ease-in-out infinite reverse;
      z-index: 1;
    }

    @keyframes floatOrb {
      0%, 100% {
        transform: translate(0, 0) scale(1);
      }
      33% {
        transform: translate(30px, -30px) scale(1.1);
      }
      66% {
        transform: translate(-20px, 20px) scale(0.9);
      }
    }

    .hero-background-overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background-image: url('https://images.unsplash.com/photo-1606811841689-23dfddce3e95?w=1920&q=80');
      background-size: cover;
      background-position: center;
      opacity: 0.08;
      z-index: 0;
    }

    /* Dot pattern overlay */
    .hero-background-overlay::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background-image: radial-gradient(circle, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
      background-size: 30px 30px;
      z-index: 1;
    }

    .hero-container {
      max-width: 1100px;
      width: 100%;
      margin: 0 auto;
      position: relative;
      z-index: 10;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
    }

    .hero-content {
      text-align: center;
      position: relative;
    }

    .hero-stats-container {
      position: relative;
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .hero-section h1 {
      font-size: clamp(42px, 5.5vw, 62px);
      font-weight: 800;
      color: #ffffff;
      line-height: 1.15;
      margin: 0 0 24px 0;
      letter-spacing: -1.8px;
      text-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
      position: relative;
    }

    .hero-subtitle {
      font-size: clamp(20px, 2.2vw, 24px);
      color: rgba(255, 255, 255, 0.95);
      line-height: 1.6;
      margin-bottom: 20px;
      font-weight: 600;
    }

    .hero-highlight {
      font-size: clamp(17px, 2vw, 19px);
      color: rgba(255, 255, 255, 0.85);
      line-height: 1.7;
      margin-bottom: 32px;
      font-style: italic;
    }

    /* Stat Cards */
    .stat-card {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      border: 3px solid rgba(36, 142, 239, 0.3);
      border-radius: 20px;
      padding: 28px 24px;
      position: absolute;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.2);
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      min-width: 240px;
    }

    .stat-card:hover {
      transform: translateY(-8px) scale(1.05);
      box-shadow: 0 30px 80px rgba(36, 142, 239, 0.3);
      border-color: var(--accent-blue);
    }

    .stat-card:nth-child(1) {
      top: 0;
      right: 0;
      animation: floatCard 6s ease-in-out infinite;
    }

    .stat-card:nth-child(2) {
      top: 50%;
      right: -20px;
      transform: translateY(-50%);
      animation: floatCard 7s ease-in-out infinite 1s;
    }

    .stat-card:nth-child(3) {
      bottom: 0;
      right: 0;
      animation: floatCard 8s ease-in-out infinite 2s;
    }

    @keyframes floatCard {
      0%, 100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-15px);
      }
    }

    .stat-icon-wrapper {
      width: 48px;
      height: 48px;
      background: linear-gradient(135deg, var(--primary-blue), var(--accent-blue));
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 12px;
    }

    .stat-icon-wrapper i {
      font-size: 24px;
      color: var(--white);
    }

    .stat-value {
      font-size: 28px;
      font-weight: 800;
      color: var(--primary-blue);
      margin-bottom: 4px;
      letter-spacing: -0.5px;
      line-height: 1;
    }

    .stat-label {
      font-size: 13px;
      color: var(--dim-grey);
      line-height: 1.4;
      font-weight: 600;
    }

    .hero-cta {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      padding: 20px 48px;
      background: var(--white);
      color: var(--primary-blue);
      font-size: 18px;
      font-weight: 700;
      border-radius: 12px;
      text-decoration: none;
      transition: all 0.3s ease;
      border: 3px solid var(--white);
      box-shadow: 0 8px 24px rgba(255, 255, 255, 0.3);
      position: relative;
      overflow: hidden;
    }

    /* Pulsing effect */
    .hero-cta::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 0;
      height: 0;
      border-radius: 50%;
      background: rgba(36, 142, 239, 0.3);
      transform: translate(-50%, -50%);
      transition: width 0.6s, height 0.6s;
    }

    .hero-cta:hover::before {
      width: 300px;
      height: 300px;
    }

    .hero-cta:hover {
      background: var(--accent-blue);
      color: var(--white);
      border-color: var(--accent-blue);
      transform: translateY(-4px);
      box-shadow: 0 12px 32px rgba(36, 142, 239, 0.4);
    }

    .hero-cta i {
      font-size: 22px;
      position: relative;
      z-index: 1;
    }

    .hero-cta span {
      position: relative;
      z-index: 1;
    }

    /* Trust badge */
    .trust-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      margin-top: 24px;
      padding: 12px 20px;
      background: rgba(255, 255, 255, 0.1);
      border: 2px solid rgba(255, 255, 255, 0.2);
      border-radius: 50px;
      color: rgba(255, 255, 255, 0.9);
      font-size: 14px;
      font-weight: 600;
    }

    .trust-badge i {
      color: var(--accent-blue);
      font-size: 18px;
    }

    /* SYSTEMS SECTION */
    .systems-section {
      padding: 120px 24px;
      background: var(--white);
    }

    .section-header {
      max-width: 800px;
      margin: 0 auto 60px;
      text-align: center;
    }

    .section-title {
      font-size: clamp(32px, 4vw, 48px);
      font-weight: 800;
      color: var(--primary-blue);
      margin-bottom: 16px;
      line-height: 1.2;
      letter-spacing: -1px;
    }

    .systems-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 32px;
      max-width: 1300px;
      margin: 0 auto;
    }

    .system-card {
      background: var(--white);
      border: 3px solid var(--grey);
      border-radius: 20px;
      padding: 40px 32px;
      transition: all 0.4s ease;
      position: relative;
      overflow: hidden;
    }

    .system-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, transparent 0%, rgba(36, 142, 239, 0.05) 100%);
      opacity: 0;
      transition: opacity 0.4s ease;
    }

    .system-card:hover {
      border-color: var(--accent-blue);
      transform: translateY(-8px);
      box-shadow: 0 16px 48px rgba(0, 0, 0, 0.12);
    }

    .system-card:hover::before {
      opacity: 1;
    }

    .system-icon {
      width: 70px;
      height: 70px;
      background: linear-gradient(135deg, var(--primary-blue), var(--accent-blue));
      border-radius: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 32px;
      color: var(--white);
      margin-bottom: 24px;
      transition: all 0.4s ease;
    }

    .system-card:hover .system-icon {
      transform: scale(1.1) rotate(5deg);
    }

    .system-card h3 {
      font-size: 22px;
      font-weight: 800;
      color: var(--primary-blue);
      margin-bottom: 12px;
      letter-spacing: -0.3px;
    }

    .system-card p {
      font-size: 15px;
      color: var(--dim-grey);
      line-height: 1.7;
      margin: 0;
    }

    /* RESULTS SECTION */
    .results-section {
      padding: 120px 24px;
      background: var(--primary-light);
    }

    .results-intro {
      max-width: 900px;
      margin: 0 auto 60px;
      text-align: center;
    }

    .results-intro h2 {
      font-size: clamp(32px, 4vw, 48px);
      font-weight: 800;
      color: var(--primary-dark);
      margin-bottom: 24px;
      line-height: 1.2;
    }

    .results-intro p {
      font-size: 18px;
      color: var(--dim-grey);
      line-height: 1.7;
    }

    .results-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 40px;
      max-width: 1200px;
      margin: 0 auto;
    }

    .result-card {
      background: var(--white);
      border: 3px solid var(--grey);
      border-radius: 24px;
      padding: 48px 40px;
      transition: all 0.4s ease;
      position: relative;
    }

    .result-card:hover {
      border-color: var(--accent-blue);
      transform: translateY(-8px);
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
    }

    .result-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: linear-gradient(135deg, var(--primary-blue), var(--accent-blue));
      color: var(--white);
      padding: 10px 20px;
      border-radius: 50px;
      font-size: 13px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 24px;
    }

    .result-badge i {
      font-size: 16px;
    }

    .result-card h3 {
      font-size: 28px;
      font-weight: 800;
      color: var(--primary-blue);
      margin-bottom: 24px;
      letter-spacing: -0.5px;
    }

    .result-stats {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .stat-item {
      display: flex;
      align-items: flex-start;
      gap: 12px;
      padding: 16px;
      background: var(--primary-light);
      border-radius: 12px;
      transition: all 0.3s ease;
    }

    .stat-item:hover {
      background: var(--grey);
      transform: translateX(4px);
    }

    .stat-icon {
      width: 32px;
      height: 32px;
      background: var(--accent-blue);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      color: var(--white);
      font-size: 16px;
    }

    .stat-content {
      flex: 1;
    }

    .stat-value {
      font-size: 18px;
      font-weight: 800;
      color: var(--primary-blue);
      margin-bottom: 4px;
      letter-spacing: -0.3px;
    }

    .stat-label {
      font-size: 14px;
      color: var(--dim-grey);
      line-height: 1.4;
    }

    /* GUARANTEE SECTION */
    .guarantee-section {
      padding: 100px 24px;
      background: linear-gradient(135deg, var(--primary-blue) 0%, var(--accent-blue) 100%);
      position: relative;
      overflow: hidden;
    }

    .guarantee-content {
      max-width: 900px;
      margin: 0 auto;
      text-align: center;
      position: relative;
      z-index: 10;
    }

    .guarantee-icon {
      width: 100px;
      height: 100px;
      background: rgba(255, 255, 255, 0.15);
      border: 3px solid var(--white);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 32px;
      font-size: 48px;
      color: var(--white);
    }

    .guarantee-section h2 {
      font-size: clamp(32px, 4vw, 48px);
      font-weight: 800;
      color: var(--white);
      margin-bottom: 24px;
      line-height: 1.2;
      letter-spacing: -1px;
    }

    .guarantee-text {
      font-size: 20px;
      color: rgba(255, 255, 255, 0.95);
      line-height: 1.7;
      margin-bottom: 40px;
      max-width: 800px;
      margin-left: auto;
      margin-right: auto;
    }

    .guarantee-cta {
      display: inline-flex;
      align-items: center;
      gap: 12px;
      padding: 20px 48px;
      background: var(--white);
      color: var(--primary-blue);
      font-size: 18px;
      font-weight: 700;
      border-radius: 14px;
      text-decoration: none;
      transition: all 0.3s ease;
      border: 3px solid var(--white);
      box-shadow: 0 8px 24px rgba(255, 255, 255, 0.3);
    }

    .guarantee-cta:hover {
      background: transparent;
      color: var(--white);
      transform: translateY(-4px);
      box-shadow: 0 12px 32px rgba(255, 255, 255, 0.4);
    }

    .guarantee-cta i {
      font-size: 22px;
    }

    /* FLOATING CTA */
    .floating-cta {
      position: fixed;
      bottom: 32px;
      right: 32px;
      display: flex;
      flex-direction: column;
      gap: 16px;
      z-index: 1000;
    }

    .floating-btn {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 16px 24px;
      background: var(--primary-blue);
      color: var(--white);
      border-radius: 50px;
      text-decoration: none;
      font-weight: 700;
      font-size: 15px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
      transition: all 0.3s ease;
    }

    .floating-btn:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 32px rgba(0, 0, 0, 0.3);
      background: var(--accent-blue);
    }

    /* RESPONSIVE */
    @media (max-width: 991px) {
      .hero-section {
        padding: 140px 24px 80px;
        min-height: 75vh;
      }

      .hero-stats-container {
        position: relative;
        height: 300px;
      }

      .stat-card {
        min-width: 200px;
      }

      .stat-card:nth-child(2) {
        right: 50%;
        transform: translate(50%, -50%);
      }

      .systems-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 24px;
      }

      .results-grid {
        grid-template-columns: 1fr;
        gap: 32px;
      }

      .systems-section, .results-section, .guarantee-section {
        padding: 80px 24px;
      }
    }

    @media (max-width: 767px) {
      .hero-section {
        padding: 130px 20px 60px;
        min-height: 70vh;
      }

      .hero-section h1 {
        font-size: clamp(32px, 7vw, 44px);
        margin-bottom: 20px;
      }

      .hero-subtitle {
        font-size: 18px;
      }

      .hero-highlight {
        font-size: 16px;
      }

      .hero-cta {
        padding: 18px 36px;
        font-size: 16px;
        width: 100%;
        max-width: 400px;
        justify-content: center;
      }

      .hero-stats-container {
        height: 250px;
        margin-top: 20px;
      }

      .stat-card {
        padding: 20px 16px;
        min-width: 180px;
      }

      .stat-card:nth-child(1) {
        top: 10px;
        right: 10px;
      }

      .stat-card:nth-child(3) {
        bottom: 10px;
        right: 10px;
      }

      .stat-value {
        font-size: 22px;
      }

      .stat-label {
        font-size: 12px;
      }

      .trust-badge {
        font-size: 13px;
        padding: 10px 16px;
      }

      .systems-grid {
        grid-template-columns: 1fr;
        gap: 20px;
      }

      .system-card {
        padding: 32px 24px;
      }

      .system-icon {
        width: 60px;
        height: 60px;
        font-size: 28px;
      }

      .result-card {
        padding: 36px 28px;
      }

      .result-card h3 {
        font-size: 24px;
      }

      .stat-value {
        font-size: 16px;
      }

      .guarantee-icon {
        width: 80px;
        height: 80px;
        font-size: 40px;
      }

      .guarantee-text {
        font-size: 17px;
      }

      .guarantee-cta {
        padding: 18px 36px;
        font-size: 16px;
        width: 100%;
        justify-content: center;
      }

      .systems-section, .results-section, .guarantee-section {
        padding: 60px 20px;
      }

      .floating-cta {
        bottom: 20px;
        right: 20px;
        gap: 12px;
      }

      .floating-btn {
        padding: 14px 20px;
        font-size: 14px;
      }

      .floating-btn span {
        display: none;
      }
    }

    @media (max-width: 479px) {
      .hero-section {
        padding: 120px 16px 50px;
      }

      .hero-section h1 {
        font-size: clamp(28px, 8vw, 38px);
      }

      .system-card h3 {
        font-size: 20px;
      }

      .result-card h3 {
        font-size: 22px;
      }

      .guarantee-section h2 {
        font-size: 28px;
      }
    }
  </style>
</head>
<body>
  <Navigation />

  <!-- HERO SECTION -->
  <section class="hero-section">
    <div class="hero-background-overlay"></div>

    <div class="hero-container">
      <div class="hero-content">
        <h1>All-On-X AI</h1>

        <p class="hero-subtitle">
          The #1 Way to Consistently Close Full Arch + Multi-Implant Cases
        </p>

        <p class="hero-highlight">
          We focus on lead quality, not quantity—bringing you patients financially qualified and ready to move forward with treatment.
        </p>

        <a href="/book-a-call" class="hero-cta">
          <span>Speak to Our Team</span>
          <i class="ti ti-arrow-right"></i>
        </a>

        <!-- <div class="trust-badge">
          <i class="ti ti-shield-check"></i>
          <span>Performance-Backed Guarantee</span>
        </div> -->
      </div>

      <!-- <div class="hero-stats-container">
        <div class="stat-card">
          <div class="stat-icon-wrapper">
            <i class="ti ti-users"></i>
          </div>
          <div class="stat-value">15+</div>
          <div class="stat-label">Accepted Cases in 90 Days</div>
        </div>

        <div class="stat-card">
          <div class="stat-icon-wrapper">
            <i class="ti ti-cash"></i>
          </div>
          <div class="stat-value">$300K-$700K</div>
          <div class="stat-label">Total Revenue Generated</div>
        </div>

        <div class="stat-card">
          <div class="stat-icon-wrapper">
            <i class="ti ti-percentage"></i>
          </div>
          <div class="stat-value">75%</div>
          <div class="stat-label">Case Acceptance Rate</div>
        </div>
      </div> -->
    </div>
  </section>

  <!-- SYSTEMS SECTION -->
  <section class="systems-section">
    <div class="section-container">
      <div class="section-header">
        <h2 class="section-title">One Integrated System Working for You</h2>
      </div>

      <div class="systems-grid">
        
        <div class="system-card">
          <div class="system-icon">
            <i class="ti ti-target"></i>
          </div>
          <h3>Targeted Marketing</h3>
          <p>
            Paid Ads to acquire your first few cases, then your patients fund your growth.
          </p>
        </div>

        <div class="system-card">
          <div class="system-icon">
            <i class="ti ti-robot"></i>
          </div>
          <h3>Instant AI Engagement</h3>
          <p>
            Our Call Center Reps reach out to your leads within 2 minutes and are available 12 hours a day. Our Voice Receptionist Maria is there after hours 24/7.
          </p>
        </div>

        <div class="system-card">
          <div class="system-icon">
            <i class="ti ti-checklist"></i>
          </div>
          <h3>Smart Qualification</h3>
          <p>
            Lead scoring based on financial capability, urgency, fit. Qualification criteria customized to the financing options your practice offers.
          </p>
        </div>

        <div class="system-card">
          <div class="system-icon">
            <i class="ti ti-refresh"></i>
          </div>
          <h3>Automated Nurturing</h3>
          <p>
            43-touchpoint sequences via SMS, email, voice to keep leads engaged and your practice top of mind.
          </p>
        </div>

        <div class="system-card">
          <div class="system-icon">
            <i class="ti ti-calendar-check"></i>
          </div>
          <h3>Direct Scheduling</h3>
          <p>
            Auto Booking right into your calendar. Maria handles appointment reminders and confirmations.
          </p>
        </div>

        <div class="system-card">
          <div class="system-icon">
            <i class="ti ti-chart-line"></i>
          </div>
          <h3>Case Acceptance Support</h3>
          <p>
            Training and systems to close consultations.
          </p>
        </div>

      </div>
    </div>
  </section>

  <!-- RESULTS SECTION -->
  <section class="results-section">
    <div class="section-container">
      <div class="results-intro">
        <h2>What To Expect</h2>
        <p>Real Results from Real Practices</p>
      </div>

      <div class="results-grid">
        
        <!-- Initial Trial -->
        <div class="result-card">
          <div class="result-badge">
            <i class="ti ti-rocket"></i>
            Initial Trial
          </div>
          <h3>First 90 Days</h3>
          <div class="result-stats">
            <div class="stat-item">
              <div class="stat-icon">
                <i class="ti ti-users"></i>
              </div>
              <div class="stat-content">
                <div class="stat-value">At least 15 Accepted Cases</div>
                <div class="stat-label">Multi-implant cases accepted in 90 days</div>
              </div>
            </div>

            <div class="stat-item">
              <div class="stat-icon">
                <i class="ti ti-cash"></i>
              </div>
              <div class="stat-content">
                <div class="stat-value">$300K - $700K</div>
                <div class="stat-label">Total earnings in initial trial period</div>
              </div>
            </div>

            <div class="stat-item">
              <div class="stat-icon">
                <i class="ti ti-clock"></i>
              </div>
              <div class="stat-content">
                <div class="stat-value">11-14 Days</div>
                <div class="stat-label">Time to first qualified consultation</div>
              </div>
            </div>

            <div class="stat-item">
              <div class="stat-icon">
                <i class="ti ti-trending-up"></i>
              </div>
              <div class="stat-content">
                <div class="stat-value">8-10x ROI</div>
                <div class="stat-label">Return on your marketing investment</div>
              </div>
            </div>
          </div>
        </div>

        <!-- Post Trial -->
        <div class="result-card">
          <div class="result-badge">
            <i class="ti ti-star"></i>
            Post Trial Performance
          </div>
          <h3>Sustained Growth</h3>
          <div class="result-stats">
            <div class="stat-item">
              <div class="stat-icon">
                <i class="ti ti-calendar-event"></i>
              </div>
              <div class="stat-content">
                <div class="stat-value">7-25 Accepted Cases/Month</div>
                <div class="stat-label">Multi-implant cases accepted each month</div>
              </div>
            </div>

            <div class="stat-item">
              <div class="stat-icon">
                <i class="ti ti-check"></i>
              </div>
              <div class="stat-content">
                <div class="stat-value">75% Case Acceptance Rate</div>
                <div class="stat-label">Consultations converting to accepted cases</div>
              </div>
            </div>

            <div class="stat-item">
              <div class="stat-icon">
                <i class="ti ti-moneybag"></i>
              </div>
              <div class="stat-content">
                <div class="stat-value">$150K - $750K</div>
                <div class="stat-label">Monthly earnings from qualified patients</div>
              </div>
            </div>

            <div class="stat-item">
              <div class="stat-icon">
                <i class="ti ti-infinity"></i>
              </div>
              <div class="stat-content">
                <div class="stat-value">Ongoing</div>
                <div class="stat-label">Sustained, predictable growth</div>
              </div>
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- GUARANTEE SECTION -->
  <section class="guarantee-section">
    <div class="guarantee-content">
      <div class="guarantee-icon">
        <i class="ti ti-shield-check"></i>
      </div>
      
      <h2>Performance-Backed Guarantee</h2>
      
      <p class="guarantee-text">
        Earn your ad spend and investment back or be completely refunded.
      </p>
      
      <a href="/book-a-call" class="guarantee-cta">
        <i class="ti ti-calendar-event"></i>
        Book a Call
      </a>
    </div>
  </section>

  <FooterBar />

  <!-- FLOATING CTA -->
  <div class="floating-cta">
    <a href="/book-a-call" class="floating-btn">
      <i class="ti ti-calendar-event" style="font-size: 24px;"></i>
      <span>Let's Talk</span>
    </a>
    <a href="tel:+16505578269" class="floating-btn">
      <i class="ti ti-phone" style="font-size: 24px;"></i>
      <span>Call Now</span>
    </a>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
      };

      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            const target = entry.target as HTMLElement;
            target.style.opacity = '1';
            target.style.transform = 'translateY(0)';
          }
        });
      }, observerOptions);

      document.querySelectorAll('.system-card').forEach((card, index) => {
        const element = card as HTMLElement;
        element.style.opacity = '0';
        element.style.transform = 'translateY(30px)';
        element.style.transition = `opacity 0.6s ease ${index * 0.1}s, transform 0.6s ease ${index * 0.1}s`;
        observer.observe(card);
      });

      document.querySelectorAll('.result-card').forEach((card, index) => {
        const element = card as HTMLElement;
        element.style.opacity = '0';
        element.style.transform = 'translateY(30px)';
        element.style.transition = `opacity 0.6s ease ${index * 0.15}s, transform 0.6s ease ${index * 0.15}s`;
        observer.observe(card);
      });

      document.querySelectorAll('.stat-item').forEach((item, index) => {
        const element = item as HTMLElement;
        element.style.opacity = '0';
        element.style.transform = 'translateX(-20px)';
        element.style.transition = `opacity 0.5s ease ${index * 0.08}s, transform 0.5s ease ${index * 0.08}s`;
        observer.observe(item);
      });
    });
  </script>

</body>
</html>