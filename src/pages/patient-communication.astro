---
// patient-communication.astro - Updated
import '../css/main.css';
import Navigation from '../components/Navigation.astro';
import FooterBar from '../components/FooterBar.astro';
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Automated Patient Communication | Dentistry Growth Partners</title>
  <meta name="description" content="Keep your schedule full and your practice top of mind by reaching out to patients at key times throughout the year with messaging designed to convert.">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@latest/tabler-icons.min.css">
  <link rel="icon" type="image/png" href="/636f4050ef41c9124a12cbba/dgp-logo1.png" />
  <link rel="shortcut icon" type="image/png" href="/636f4050ef41c9124a12cbba/dgp-logo1.png" />
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --primary-light: #EDEDE8;
      --primary-dark: #051023;
      --primary-blue: #07387D;
      --accent-blue: #248EEF;
      --white: #ffffff;
      --black: #1d1d1d;
      --grey: #f5f5f5;
      --dim-grey: #646464;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: var(--white);
      color: var(--black);
      line-height: 1.6;
      overflow-x: hidden;
    }

    .section-container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 24px;
    }

    /* HERO SECTION */
    .hero-section {
      min-height: 85vh;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      overflow: hidden;
      background: linear-gradient(135deg, var(--primary-dark) 0%, var(--primary-blue) 50%, var(--accent-blue) 100%);
      padding: 160px 24px 100px;
    }

    /* Animated gradient orbs */
    .hero-section::before {
      content: '';
      position: absolute;
      top: -150px;
      right: -150px;
      width: 500px;
      height: 500px;
      background: radial-gradient(circle, rgba(36, 142, 239, 0.25) 0%, transparent 70%);
      border-radius: 50%;
      animation: floatOrb 20s ease-in-out infinite;
      z-index: 1;
    }

    .hero-section::after {
      content: '';
      position: absolute;
      bottom: -100px;
      left: -100px;
      width: 400px;
      height: 400px;
      background: radial-gradient(circle, rgba(36, 142, 239, 0.2) 0%, transparent 70%);
      border-radius: 50%;
      animation: floatOrb 25s ease-in-out infinite reverse;
      z-index: 1;
    }

    @keyframes floatOrb {
      0%, 100% {
        transform: translate(0, 0) scale(1);
      }
      33% {
        transform: translate(30px, -30px) scale(1.1);
      }
      66% {
        transform: translate(-20px, 20px) scale(0.9);
      }
    }

    .hero-background-overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background-image: url('https://images.unsplash.com/photo-1629909613654-28e377c37b09?w=1920&q=80');
      background-size: cover;
      background-position: center;
      opacity: 0.08;
      z-index: 0;
    }

    /* Dot pattern overlay */
    .hero-background-overlay::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background-image: radial-gradient(circle, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
      background-size: 30px 30px;
      z-index: 1;
    }

    .hero-container {
      max-width: 1100px;
      width: 100%;
      margin: 0 auto;
      position: relative;
      z-index: 10;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
    }

    .hero-content {
      text-align: center;
      position: relative;
    }

    .hero-section h1 {
      font-size: clamp(42px, 5.5vw, 62px);
      font-weight: 800;
      color: #ffffff;
      line-height: 1.15;
      margin: 0 0 24px 0;
      letter-spacing: -1.8px;
      text-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
      position: relative;
    }

    .hero-subtitle {
      font-size: clamp(20px, 2.2vw, 24px);
      color: rgba(255, 255, 255, 0.95);
      line-height: 1.6;
      margin-bottom: 0;
      font-weight: 600;
    }

    /* WHAT IT DOES SECTION */
    .what-it-does-section {
      padding: 120px 24px;
      background: var(--white);
    }

    .section-header {
      max-width: 900px;
      margin: 0 auto 60px;
      text-align: center;
    }

    .section-title {
      font-size: clamp(32px, 4vw, 48px);
      font-weight: 800;
      color: var(--primary-blue);
      margin-bottom: 20px;
      line-height: 1.2;
      letter-spacing: -1px;
    }

    .section-description {
      font-size: 19px;
      color: var(--dim-grey);
      line-height: 1.7;
    }

    .features-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 32px;
      max-width: 1200px;
      margin: 0 auto;
    }

    .feature-card {
      background: var(--white);
      border: 3px solid var(--grey);
      border-radius: 20px;
      padding: 40px 36px;
      transition: all 0.4s ease;
      position: relative;
      overflow: hidden;
    }

    .feature-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, transparent 0%, rgba(36, 142, 239, 0.05) 100%);
      opacity: 0;
      transition: opacity 0.4s ease;
    }

    .feature-card:hover {
      border-color: var(--accent-blue);
      transform: translateY(-8px);
      box-shadow: 0 16px 48px rgba(0, 0, 0, 0.12);
    }

    .feature-card:hover::before {
      opacity: 1;
    }

    .feature-icon {
      width: 70px;
      height: 70px;
      background: linear-gradient(135deg, var(--primary-blue), var(--accent-blue));
      border-radius: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 32px;
      color: var(--white);
      margin-bottom: 24px;
      transition: all 0.4s ease;
    }

    .feature-card:hover .feature-icon {
      transform: scale(1.1) rotate(5deg);
    }

    .feature-card h3 {
      font-size: 22px;
      font-weight: 800;
      color: var(--primary-blue);
      margin-bottom: 12px;
      letter-spacing: -0.3px;
    }

    .feature-card p {
      font-size: 15px;
      color: var(--dim-grey);
      line-height: 1.7;
      margin: 0;
    }

    /* BENEFITS SECTION */
    .benefits-section {
      padding: 100px 24px;
      background: var(--primary-light);
    }

    .benefits-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 40px;
      max-width: 1100px;
      margin: 60px auto 0;
    }

    .benefit-card {
      background: var(--white);
      border: 3px solid var(--grey);
      border-radius: 24px;
      padding: 48px 40px;
      transition: all 0.4s ease;
      text-align: center;
    }

    .benefit-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
      border-color: var(--accent-blue);
    }

    .benefit-icon {
      width: 80px;
      height: 80px;
      background: linear-gradient(135deg, var(--primary-blue), var(--accent-blue));
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 24px;
      font-size: 36px;
      color: var(--white);
      transition: all 0.4s ease;
    }

    .benefit-card:hover .benefit-icon {
      transform: scale(1.1) rotate(5deg);
    }

    .benefit-card h3 {
      font-size: 22px;
      font-weight: 800;
      color: var(--primary-blue);
      margin-bottom: 0;
    }

    /* FINAL CTA */
    .final-cta-section {
      padding: 100px 24px;
      background: linear-gradient(135deg, var(--primary-blue) 0%, var(--accent-blue) 100%);
      position: relative;
      overflow: hidden;
    }

    .final-cta-content {
      max-width: 900px;
      margin: 0 auto;
      text-align: center;
      position: relative;
      z-index: 10;
    }

    .final-cta-icon {
      width: 100px;
      height: 100px;
      background: rgba(255, 255, 255, 0.15);
      border: 3px solid var(--white);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 32px;
      font-size: 48px;
      color: var(--white);
    }

    .final-cta-section h2 {
      font-size: clamp(32px, 4vw, 48px);
      font-weight: 800;
      color: var(--white);
      margin-bottom: 24px;
      line-height: 1.2;
      letter-spacing: -1px;
    }

    .final-cta-text {
      font-size: 20px;
      color: rgba(255, 255, 255, 0.95);
      line-height: 1.7;
      margin-bottom: 40px;
      max-width: 800px;
      margin-left: auto;
      margin-right: auto;
    }

    .final-cta-button {
      display: inline-flex;
      align-items: center;
      gap: 12px;
      padding: 20px 48px;
      background: var(--white);
      color: var(--primary-blue);
      font-size: 18px;
      font-weight: 700;
      border-radius: 14px;
      text-decoration: none;
      transition: all 0.3s ease;
      border: 3px solid var(--white);
      box-shadow: 0 8px 24px rgba(255, 255, 255, 0.3);
    }

    .final-cta-button:hover {
      background: transparent;
      color: var(--white);
      transform: translateY(-4px);
      box-shadow: 0 12px 32px rgba(255, 255, 255, 0.4);
    }

    .final-cta-button i {
      font-size: 22px;
    }

    /* FLOATING CTA */
    .floating-cta {
      position: fixed;
      bottom: 32px;
      right: 32px;
      display: flex;
      flex-direction: column;
      gap: 16px;
      z-index: 1000;
    }

    .floating-btn {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 16px 24px;
      background: var(--primary-blue);
      color: var(--white);
      border-radius: 50px;
      text-decoration: none;
      font-weight: 700;
      font-size: 15px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
      transition: all 0.3s ease;
    }

    .floating-btn:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 32px rgba(0, 0, 0, 0.3);
      background: var(--accent-blue);
    }

    /* RESPONSIVE */
    @media (max-width: 991px) {
      .hero-section {
        padding: 140px 24px 80px;
        min-height: 75vh;
      }

      .features-grid, .benefits-grid {
        grid-template-columns: 1fr;
        gap: 24px;
      }

      .what-it-does-section, .benefits-section, .final-cta-section {
        padding: 80px 24px;
      }
    }

    @media (max-width: 767px) {
      .hero-section {
        padding: 130px 20px 60px;
        min-height: 70vh;
      }

      .hero-section h1 {
        font-size: clamp(32px, 7vw, 44px);
        margin-bottom: 20px;
      }

      .hero-subtitle {
        font-size: 18px;
      }

      .feature-card, .benefit-card {
        padding: 36px 28px;
      }

      .feature-icon {
        width: 60px;
        height: 60px;
        font-size: 28px;
      }

      .benefit-icon {
        width: 70px;
        height: 70px;
        font-size: 32px;
      }

      .final-cta-icon {
        width: 80px;
        height: 80px;
        font-size: 40px;
      }

      .final-cta-text {
        font-size: 17px;
      }

      .final-cta-button {
        padding: 18px 36px;
        font-size: 16px;
        width: 100%;
        justify-content: center;
      }

      .what-it-does-section, .benefits-section, .final-cta-section {
        padding: 60px 20px;
      }

      .floating-cta {
        bottom: 20px;
        right: 20px;
        gap: 12px;
      }

      .floating-btn {
        padding: 14px 20px;
        font-size: 14px;
      }

      .floating-btn span {
        display: none;
      }
    }

    @media (max-width: 479px) {
      .hero-section {
        padding: 120px 16px 50px;
      }

      .hero-section h1 {
        font-size: clamp(28px, 8vw, 38px);
      }

      .feature-card h3 {
        font-size: 20px;
      }

      .benefit-card h3 {
        font-size: 20px;
      }

      .final-cta-section h2 {
        font-size: 28px;
      }
    }
  </style>
</head>
<body>
  <Navigation />
  
  <!-- HERO SECTION -->
  <section class="hero-section">
    <div class="hero-background-overlay"></div>

    <div class="hero-container">
      <div class="hero-content">
        <h1>Automated Patient Communication</h1>

        <p class="hero-subtitle">
          Keep your schedule full and your practice top of mind by reaching out to patients at key times throughout the year with messaging designed to convert.
        </p>
      </div>
    </div>
  </section>

  <!-- WHAT IT DOES SECTION -->
  <section class="what-it-does-section">
    <div class="section-container">
      <div class="section-header">
        <h2 class="section-title">Keep Your Patients Coming Through the Door</h2>
        <p class="section-description">
          Your practice stays top of mind of all your patients when you automatically reach out throughout the year on special dates, with relevant practice updates, and promo offers. Increase how often and how likely your patients self-book their own appointments.
        </p>
      </div>

      <div class="features-grid">
        
        <div class="feature-card">
          <div class="feature-icon">
            <i class="ti ti-gift"></i>
          </div>
          <h3>Automated Birthday & Holiday Messages</h3>
          <p>
            Send personalized greetings on holidays, birthdays and special occasions - accounting for at least 12 touchpoints through the year to build likeness and loyalty.
          </p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <i class="ti ti-phone-call"></i>
          </div>
          <h3>Post-Treatment Review & Follow Up</h3>
          <p>
            Have Maria call post-procedure to see how their experience was with your practice. By checking in, it shows you care and provides the opportunity to add another 5 star review to your google page. Maria even requests referrals on your behalf, turning a patient today into a patient tomorrow.
          </p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <i class="ti ti-refresh-alert"></i>
          </div>
          <h3>Patient Database Reactivation</h3>
          <p>
            Mine for hidden interest sitting in your PMS and turn dormant contacts into new appointments and filling schedule gaps.
          </p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <i class="ti ti-star"></i>
          </div>
          <h3>Review + Referral Request Automation</h3>
          <p>
            Build your online reputation with 5 star reviews and generate referrals with strategic promo offers to your existing patient base.
          </p>
        </div>

      </div>
    </div>
  </section>

  <!-- BENEFITS SECTION -->
  <section class="benefits-section">
    <div class="section-container">
      <div class="section-header">
        <h2 class="section-title">Benefits</h2>
      </div>

      <div class="benefits-grid">
        
        <div class="benefit-card">
          <div class="benefit-icon">
            <i class="ti ti-percentage"></i>
          </div>
          <h3>Reduce No-Shows by Up to 80%</h3>
        </div>

        <div class="benefit-card">
          <div class="benefit-icon">
            <i class="ti ti-trending-up"></i>
          </div>
          <h3>Increase Patient Lifetime Value</h3>
        </div>

        <div class="benefit-card">
          <div class="benefit-icon">
            <i class="ti ti-users"></i>
          </div>
          <h3>Free Staff from Manual Follow-Up</h3>
        </div>

        <div class="benefit-card">
          <div class="benefit-icon">
            <i class="ti ti-check"></i>
          </div>
          <h3>Consistent Communication Touchpoints</h3>
        </div>

      </div>
    </div>
  </section>

  <!-- FINAL CTA SECTION -->
  <section class="final-cta-section">
    <div class="final-cta-content">
      <div class="final-cta-icon">
        <i class="ti ti-message-circle-check"></i>
      </div>
      
      <h2>Ready to Automate Your Patient Communication?</h2>
      
      <p class="final-cta-text">
        Stop losing patients to poor follow-up. Let intelligent automation keep your patients engaged, your schedule full, and your team focused on what matters most.
      </p>
      
      <a href="/book-a-call" class="final-cta-button">
        <i class="ti ti-calendar-event"></i>
        Reach Out to Us Now
      </a>
    </div>
  </section>

  <FooterBar />

  <!-- FLOATING CTA -->
  <div class="floating-cta">
    <a href="/book-a-call" class="floating-btn">
      <i class="ti ti-calendar-event" style="font-size: 24px;"></i>
      <span>Let's Talk</span>
    </a>
    <a href="tel:+16505578269" class="floating-btn">
      <i class="ti ti-phone" style="font-size: 24px;"></i>
      <span>Call Now</span>
    </a>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
      };

      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            const target = entry.target as HTMLElement;
            target.style.opacity = '1';
            target.style.transform = 'translateY(0)';
          }
        });
      }, observerOptions);

      document.querySelectorAll('.feature-card').forEach((card, index) => {
        const element = card as HTMLElement;
        element.style.opacity = '0';
        element.style.transform = 'translateY(30px)';
        element.style.transition = `opacity 0.6s ease ${index * 0.1}s, transform 0.6s ease ${index * 0.1}s`;
        observer.observe(card);
      });

      document.querySelectorAll('.benefit-card').forEach((card, index) => {
        const element = card as HTMLElement;
        element.style.opacity = '0';
        element.style.transform = 'translateY(30px)';
        element.style.transition = `opacity 0.6s ease ${index * 0.15}s, transform 0.6s ease ${index * 0.15}s`;
        observer.observe(card);
      });
    });
  </script>

</body>
</html>